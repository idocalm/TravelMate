<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.Theme.TravelMate" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorOnSurface">#181717</item>
        <item name="colorPrimary">@color/green_dark</item>
        <item name="colorAccent">@color/green</item>

        <item name="colorControlActivated">@color/green</item> <!-- Focused underline -->
        <item name="colorControlNormal">@color/green_dark</item>     <!-- Unfocused underline -->
        <item name="colorControlHighlight">@color/green</item> <!-- Ripple/highlight -->

        <item name="android:windowSplashScreenAnimatedIcon">@null</item>
        <item name="android:windowSplashScreenBackground">@android:color/transparent</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="android:windowSplashScreenBrandingImage">@null</item>

    </style>
</resources>